"use strict"   //used for treating our code by newest version
//alert("arpit") //can't use on nodejs but use in browser
console.log("arpit")
const id="1234"   //declaring constants
let nam="arpit"    //dec variables
var clas="nothing"    //also varible
let college="KIT";
console.log(id)  //printing values
console.table([id ,nam, clas,college ]) //printing in table
// we use let instead of var because problem of block scope
console.log(typeof "nothing")
console.log(typeof null)
console.log(typeof undefined)

//data types in js     primitive datatype(call by value) = int , big int , string , null,undefined,symbol , boolean ;
//                   non primitive(call by referance)= array,objects,functions

// It store string data type
let txt = "GeeksforGeeks";

// It store integer data type
let a = 5;
let b = 5;

// It store Boolean data type
(a == b )

// To check Strictly (i.e. Whether the datatypes
// of both variables are same) === is used
//(a === b)---> returns true to the console
 
// It store array data type
let places= ["GFG", "Computer", "Hello"];

// It store object data (objects are 
// represented in the below way mainly)
let Student = {
    firstName: "Johnny",
    lastName: "Diaz", 
    age: 35, 
    mark: "blueEYE"
}
console.log(student)
